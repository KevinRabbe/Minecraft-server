# Architecture Completion Gate

This file tracks the architecture-first requirement before remaining gameplay feature implementation proceeds.

Status values:

- **PROVEN** — architecture exists and repository implementation/tests demonstrate the authority boundary. A PROVEN common authority may still need additional Paper/UI/runtime adapters before a player can exercise every path.
- **CONTRACTED** — detailed architecture contract is defined; implementation may still be pending at one or more authority boundaries.
- **PENDING** — architecture contract is missing/contradictory; feature code must not proceed past this boundary.

## Cross-cutting architecture matrix

| Area | Status | Canonical contract / evidence |
|---|---|---|
| Stable player identity | PROVEN | `AUTHORITY_MODEL.md`, player identity/session repositories and integration tests |
| Single-writer session ownership/version fencing | PROVEN | `PLAYER_STATE_AND_TRANSFERS.md`, session/transfer integration tests, Paper authoritative-state mutation lane, plus bounded reconciliation of live session↔player-state versions, lifecycle custody, owner-instance/backend identity, open transfer tickets and routed target identity |
| Zone/backend routing | PROVEN | `WORLD_ZONES_INSTANCES.md`, control/transfer repositories, and deterministic churn coverage proving routing excludes STARTING/DRAINING/STOPPED instances, hard-full instances, stale instance heartbeats, stale/draining backends, while moving back to a recovered target only after fresh authoritative heartbeat state; single-active-instance bootstrap attachment is implemented while autoscaling/multi-instance creation policy remains explicit future routing work |
| PostgreSQL migrations/operation locking | PROVEN | `DATA_MODEL.md`, `TRANSACTIONS_AND_ANTI_DUPE.md`, migration/operation-lock code, JDBC fault injection proving exact retry after PostgreSQL commits successfully but the caller loses the commit acknowledgement for both Coin and a multi-authority item-delivery claim, plus mid-transaction fault injection proving already-executed player/session writes roll back when the later item-custody update fails before commit |
| Unique-item identity/custody/provenance | PROVEN | `ITEMS_AND_INVENTORY.md`, unique-item authority/provenance tests, recursive Paper validation of managed identities nested in supported player-owned bundle/container ItemStacks, and fail-closed Paper boundaries preventing managed value from drifting into untracked vanilla crafting, world Item entities, external containers/automation, item frames, armor stands, lecterns, flower pots, world block placement, Creative inventory mutation, unsupported nested transform/consumption state, native non-block right-click consumption/transformation, vanilla animal-feed/breeding consumption, direct composter insertion, or mutable vanilla durability/Mending before those mechanics have explicit persistent authority |
| Pending unique delivery | PROVEN | pending-delivery authority/tests plus generic Paper materialization through fenced serialized player state |
| Shared commodity-delivery claim consumption | PROVEN | generic `COMMODITY_DELIVERY_CLAIM` authority plus bounded reconciliation of CLAIMED delivery identity/quantity to the exact processed claim, exact expected→resulting player-state version step, referenced session/player evidence and monotonic current authority; source-specific issuance remains reconciled by the originating Bazaar/Secure Trade/clan storage/crafting/PvE system rather than duplicated here |
| Shared unique-delivery claim consumption | PROVEN | generic `PENDING_UNIQUE_DELIVERY_CLAIM` authority plus bounded reconciliation of CLAIMED delivery identity to the exact processed claim, immutable DELIVERED provenance, and monotonic item/player/session versions; PENDING rows still require live pending custody, while historical CLAIMED rows remain valid after legitimate later trade/storage/quarantine/other item movement |
| Coin wallet + append-only economic evidence | PROVEN | `ECONOMY.md`, wallet/evidence repositories/tests |
| PvE pocket-Coin death-loss authority | PROVEN | `ECONOMY.md`; version-identified loss policy is evaluated only after locking the current pocket wallet, then one idempotent `PVE_DEATH_LOSS` result freezes pre/post balance, wallet versions and destroyed amount; positive loss has one exact Coin sink ledger line, zero-loss outcomes remain idempotent without a fake wallet mutation, protected Bank custody is excluded, and bounded integrity + Coin-flow analytics reconcile the result. Paper now has a duplicate-fenced ordinary persistent-world death adapter behind strict versioned configuration, excluding direct player kills and managed disposable Map encounters. The bundled policy remains disabled; exact launch percentage/curve and wider death consequences remain explicit tuning/content decisions. |
| Fixed-price Auction House custody/settlement | PROVEN | `ECONOMY.md`, AH authority/tests and player-facing browse/list/buy/cancel bridge plus bounded reconciliation of listing-create operation/provenance/ledger evidence and SOLD/CANCELLED processed settlement, pending-delivery, provenance and exact Coin/item ledgers |
| Protected Bank Manager semantics | PROVEN | Bank account/tier/transfer/upgrade/interest authority and integration tests plus bounded reconciliation of immutable Bank operation history, Bank state-version/balance/tier/interest state, operation-specific Coin ledger evidence, and current loaded-tier identity/capacity |
| Bazaar order-book semantics | PROVEN | buy/sell reserve, price-time matching, partial fill, cancellation, delivery and concurrency tests plus bounded reconciliation of creation escrow, immutable fill→buyer-delivery evidence, reconstructed remainder/reserve/status and exact BUY/SELL cancellation returns; current schema intentionally has no invented fill→match-pass link |
| Secure direct-trade settlement | PROVEN | Coin/commodity/individualized-item escrow, revision/confirmation locking and terminal settlement/cancellation authority plus bounded reconstruction of exact terminal processed result, frozen escrow→recipient delivery set, pending issuance, unique-item provenance and terminal Coin/commodity/item CREDIT ledgers; trade creation intentionally uses `create_operation_id` row identity rather than an invented processed-create result |
| Irreversible salvage settlement | PROVEN | `SALVAGE_INTEGRITY.md`; session-fenced serialized-state removal + unique-item destruction + configured Coin/commodity returns, with bounded reconstruction of the exact processed request/result, state versions, destruction provenance, ledger lines, durable return deliveries and deterministic child source-operation identities |
| Crafting exactly-once settlement | PROVEN | personal crafting + commissions, exact ingredient-state verification, persistent commodity/individual output issuance, recoverable Crafting-XP, Paper `/craft` bridge, managed-item vanilla crafting-table bypass fence, and bounded integrity reconciliation of immutable personal/commission source operations, creation-time output evidence and fulfilled Crafting-XP evidence |
| Persistent normalized rolled-item quality | PROVEN | definition-owned bounded roll profiles, immutable normalized roll state, validated runtime snapshots/cache, derived Paper/AH presentation, conservative delivery projection and fail-closed intrinsic-damage attribute materialization on join/delivery |
| Equipment upgrade/salvage separation | PROVEN authority | V82 upgrade evidence/integrity, session-fenced atomic carried-item + serialized-state upgrade transition, replay/concurrency/category guards; irreversible salvage authority remains a separate one-way value path; exact upgrade economics/power remain content decisions |
| Item use/equip requirements | PROVEN foundation | definition-owned use requirements, catalog validation, bounded fail-closed Paper projection, reconnect/refresh fencing and committed-XP projection updates; action-level skill enforcement remains intentionally dormant until launch content opts into a real requirement. Independently, unmanaged vanilla non-block right-click consumption/transformation is fail-closed for managed value until a feature adapter owns that state transition. |
| Generic skills + staged active caps 50/75/100 | PROVEN | skill authority, cap transitions, no hidden above-cap XP, mutable-state↔append-only XP reconciliation, current catalog/cap ceilings, staged-cap source/version reconciliation to append-only `SKILL_CAP_ADVANCE` evidence, and a pre-registration startup gate requiring every skill ID referenced by current state or append-only XP evidence to remain loaded while allowing curve retuning only when the active-cap ceiling still represents all durable XP |
| Authorized gathering source boundary | PROVEN | `RESOURCE_SOURCE_INTEGRITY.md`; renewable source-cycle authority, fulfillment recovery, Paper Mining/Woodcutting/Farming bridge, restart-derived visual state, and bounded reconciliation of source heads, immutable harvest operations, commodity/optional-XP fulfillment evidence and recoverable unfinished fulfillment |
| Authorized ordinary-PvE entity source boundary | PROVEN | `RESOURCE_SOURCE_INTEGRITY.md`; source-cycle→spawn-ID→entity-UUID binding, exact kill claim, no-reward death/expiry recovery, managed Zombie Paper bridge, bounded reconstruction of prepared claim→KILLED spawn→immutable resource harvest evidence, and a high-cardinality persistence burst proving 64 independent sources through 128 rewarded lifecycle chains under connection-pool contention; real Paper TPS/MSPT/client rendering remain empirical |
| Portal/Map run lifecycle | PROVEN | exact Map item provenance, inventory-coupled one-Map→one-run opening, disposable reservation/handoff, managed encounter runtime, completion/abandon recovery and return routing, plus bounded reconstruction of state-coupled open→reservation→pinned-transfer handoff and CREATED/ACTIVE/COMPLETED/FAILED run evidence; pre-handoff CREATED/BOUND and terminal BOUND-before-release states remain recoverable rather than corrupt |
| Map difficulty/version/reward semantics | PROVEN | `MAP_REWARD_INTEGRITY.md`; immutable run definition, reward settlement/grant/fulfillment authorities, successor-Map issuance and retry tests, bounded reconciliation from the completed run reward operation through participant-bound ordered grants into exact commodity/unique/Map delivery evidence and immutable successor-Map profile, plus exact Paper materialization compatibility: objective/generation/modifier mechanics fail closed unless implemented, while `balance_version` selects an exact versioned encounter-content definition so historical Maps retain their frozen balance semantics |
| PvE historical clear/leaderboard source | PROVEN | immutable Map clear records with participants/configuration/world-era context plus Paper leaderboard views |
| Bounty family/tier/contract/summon/reward lifecycle | PROVEN | `BOUNTY_LIFECYCLE_INTEGRITY.md`; paid start freezes exact `(family, tier, content_version)` identity, managed-kill progress accepts only sources configured for that frozen version, and summon preparation/lease/materialization, boss identity, completion/failure and rewards continue resolving the same historical definition; PostgreSQL regression proves a v1 contract retains its v1 boss/rewards after a v2 deploy while a new contract selects v2, with bounded reconciliation of start/Coin fee, direct and managed progress, frozen content version, summon state/prepare/lease history, immutable boss materialization and terminal completion/failure evidence |
| Bounty materials/pouches/tradability | PROVEN | bounty rewards, family-pouch custody/economic path, Bazaar tradability and player-facing pouch withdrawal; completed reward evidence, pouch conservation and withdrawal delivery remain reconciled by the persistent PvE verifier |
| Clan membership/roles/leadership | PROVEN | invite/accept/cancel, one-clan membership, committed leader invariant, role changes, atomic leadership transfer, bounded clan chat and configurable shared member cap |
| Clan treasury/shared storage custody | PROVEN | Coin treasury plus commodity/individualized storage custody and permission tests/player bridge; clan Coin treasury has bounded transfer-result/version/arithmetic/current-balance/clan-ledger reconciliation, while clan storage adds bounded processed-operation shape, contiguous commodity quantity/version history, withdrawal-delivery issuance, individualized provenance and exact two-line asset-ledger reconciliation on top of current custody-vs-ledger checks |
| Ranked PvP isolation | PROVEN baseline runtime | isolated 1.8.9 dispatch/routing/admission, materialization-gated standardized 1v1 runtime, no-show/disconnect/timeout recovery and exactly-once result/rating settlement; real-client combat-feel acceptance remains deferred empirical work |
| Clan-war economic custody/settlement | PROVEN baseline runtime | challenge/roster/custody/frozen loadout, sealed identity-free runtime transport, baseline 1.8.9 control-point runtime, recovery and exactly-once rating/result settlement; broader gear representation and real-client acceptance remain deferred |
| Expansion voting/ballot/result authority | PROVEN | immutable candidates, effective ballots, authoritative close/tie/cancel/runoff, feature/world-era consequences, player-facing ballot/state projection, and resolved-vote reconciliation against append-only resolution evidence + Chronicle + resulting feature/era state |
| Feature state/world era | PROVEN | expansion result + serialized world-era authority/history; resolved expansion consequences are cross-checked against winning feature availability and exact resulting-era source/time |
| Ordinary player-built district no-blueprint rule | CONTRACTED | `WORLD_VOTING_AND_HISTORY.md`, `COMMUNITY_PROJECTS.md`, `WORLD_ZONES_INSTANCES.md` |
| Optional explicit Community Project boundary | CONTRACTED | `COMMUNITY_PROJECTS.md` |
| Hidden Artifact discovery + Attunement | PROVEN | persistent definitions/location revisions/discoveries/profile authority plus Paper interaction and `/attune` bridge; bounded integrity reconciliation verifies create/relocate/discovery source-operation evidence and current active profile identity against the loaded catalog; Paper startup also rejects a deployment before backend registration when any currently selected `active_profile_id` is absent, while tuning/stat mapping may change behind the same stable profile ID and an old ID may be retired once no player selects it |
| Chronicle/historical-event source model | PROVEN | append-only Chronicle authority, source uniqueness tests and player read projection; expansion-resolution Chronicle records are reconciled to the authoritative resolved vote |
| Persistent integrity diagnostics | PROVEN | bounded read-only aggregate verifier across session/transfer authority, Coin/economy custody, `PVE_DEATH_LOSS` processed-result/wallet-version/exact sink-ledger reconstruction, shared commodity- and unique-delivery claim consumption, Auction create/terminal settlement operation+delivery+provenance+ledger evidence, Bank operation/state/ledger evidence plus loaded-tier checks, Bazaar creation/fill/order/cancellation evidence, Secure Trade terminal operation+frozen-escrow/delivery/provenance/ledger reconstruction, irreversible salvage processed/state/provenance/ledger/return-delivery evidence, clan treasury transfer-result/version/arithmetic/current-balance/clan-ledger reconciliation, clan storage current custody plus operation/commodity-history/withdrawal-delivery/provenance/ledger reconstruction, item-upgrade definition/evidence, skill-state↔XP-evidence plus staged-cap operation reconciliation/current catalog+cap invariants, renewable resource source-head/harvest/fulfillment/managed-entity evidence, Bounty contract/start/progress/summon/materialization/terminal evidence including exact frozen content-version identity, personal/commission craft source + creation-time output + fulfilled Crafting-XP evidence, expansion resolution/history/feature/world-era consequences, Artifact create/relocate/discovery evidence and current Attunement profile identity, Map state-coupled open/reservation/pinned-handoff/run-lifecycle reconstruction, persistent PvE including completed Map reward settlement→participant grant→exact fulfillment delivery/profile reconstruction plus Bounty completion/pouch accounting, clans and competitive state; Paper `/integrity` loads bundled item/skill/Attunement/Bank-tier catalogs for catalog-aware checks; the recovered mid-transaction database-failure scenario is required to finish with the aggregate verifier returning zero issues |
| Implemented operator command isolation | PROVEN | `PERMISSIONS.md`; named `/integrity` and `/devzone` capabilities are enforced inside their executors, YAML capability wiring is regression-tested, and development routing remains additionally closed by runtime policy; Creative inventory mutation is also fenced from managed value so Creative mode is not an implicit unaudited mint/destroy/custody authority |
| Value-changing staff/recovery mutation | CONTRACTED | `PERMISSIONS.md`, `FAILURE_RECOVERY.md`; no generic persistent-value repair/mint/override command exists and any future operation requires narrow authorization plus append-only audit evidence |
| Configuration/version validation | PROVEN for current content lanes | strict item/skill/resource/crafting/attunement/placement/death-loss loaders; exact Map encounter objective/generation/modifier compatibility and versioned balance-content lookup; exact historical Bounty `(family,tier,content_version)` lookup; and pre-registration live-state compatibility gates for active Bounty contracts, unresolved Map obligations, crafting commissions/unfulfilled Crafting-XP, current Bank tiers/capacities, currently selected Attunement profile IDs, and durable skill IDs/XP ceilings; broader operational configuration remains contracted |
| Analytics/observability separation from authority | PROVEN baseline | `ANALYTICS.md`; read-only session analytics derives observed player-time, unique/new/returning participation and first-session cohort retention from authoritative `player_sessions`; Coin-flow analytics explicitly classifies current faucet/sink/custody operation types including committed `PVE_DEATH_LOSS`, derives Bazaar fee destruction from append-only `BAZAAR_MATCH` processed-operation evidence, and exposes unknown/malformed Coin-bearing operations as unclassified instead of treating raw escrow ledger net as supply; read-only Bazaar market analytics derives current best bid/ask, best-level + total open depth, and observation-fenced match/fill quantity/value/fees from authoritative `bazaar_orders` plus append-only `BAZAAR_MATCH` results; no duplicate analytics lifecycle, currency ledger or market authority is created |
| Backup/restore/failure semantics | CONTRACTED | `FAILURE_RECOVERY.md`; offline coherent backup/restore tooling, checksum/version validation, incomplete-restore startup fencing and bounded proxy-first local logout drain are CI-qualified structurally; JDBC fault injection additionally proves both complete rollback of partial multi-authority writes before commit and exact idempotent recovery after a successful commit whose acknowledgement is lost, with aggregate integrity clean after recovered adversarial rollback; actual Windows/Docker restore/shutdown rehearsal remains empirical before release |
| Extension/new-content reuse rules | CONTRACTED | `EXTENSION_POINTS.md` |

## Architecture-document gate

The architecture-document layer is complete when every settled V1 system has:

1. explicit durable authority;
2. identity/custody semantics;
3. transaction/idempotency boundary where persistent state changes;
4. live-instance versus persistent-state separation;
5. configuration/version boundary;
6. failure/recovery rule;
7. permission/admin boundary;
8. acceptance/integrity proof target.

The canonical architecture layer currently contains **no PENDING product-contract rows**. Remaining CONTRACTED rows are implementation/adaptation or empirical acceptance work, not unresolved product-law contradictions.

## Current implementation frontier

The repository is now well beyond the original scaffold. Core V1 persistent authority is proven across economy, progression, Maps/Bounties, clans/competitive state, voting/history, Artifacts, gathering, ordinary-PvE source identity and individualized gear state.

Player-facing/adapter and operations work already includes:

- authoritative Mining, Woodcutting and Farming source interactions;
- durable commodity and unique-item delivery into fenced persistent inventory;
- managed ordinary-PvE Zombie and Bounty boss flows;
- hidden Artifact discovery and Attunement selection, with startup compatibility requiring every currently selected stable profile ID to remain loaded while allowing tuning changes behind that identity;
- stable skill progression with startup compatibility requiring every durable skill ID to remain loaded and any retuned curve to preserve enough active-cap XP capacity for existing state/evidence;
- personal crafting, commissions, Bank/Bazaar/Auction House/trade/salvage and clan storage/treasury surfaces;
- inventory-coupled Map opening, managed Map encounter completion/recovery and Persistent-MMO leaderboards, with fail-closed Paper compatibility for unimplemented objective/generation/modifier semantics and exact balance-version content lookup so persisted historical Maps cannot silently run under newer tuning;
- expansion voting/Chronicle projections with cross-authority resolved-vote integrity reconciliation;
- isolated Ranked 1v1 and baseline Clan-War 1.8.9 execution paths;
- validated rolled-item runtime snapshots/presentation and fail-closed intrinsic-damage combat materialization plus structurally safe carried-item upgrade authority;
- a fail-closed managed-item custody/native-state perimeter covering vanilla crafting, world drops/ground pickup, external containers/automation, item frames, armor stands, lecterns, flower pots, world block placement, Creative inventory mutation, native non-block right-click consumption/transformation, vanilla animal feeding/breeding, direct composter insertion, and vanilla durability/Mending, plus bounded recursive validation of managed identities nested inside supported bundle/container ItemStacks while unsupported vanilla transform/consumption nesting remains closed; any future reopening of these native surfaces requires an explicit feature-owned settlement/custody adapter rather than implicit Minecraft behavior;
- policy-neutral ordinary-PvE pocket-Coin death-loss authority plus a duplicate-fenced Paper adapter behind strict versioned configuration; the bundled policy remains disabled, direct player kills and managed disposable Map encounters are excluded, and the exact launch curve/wider death consequences intentionally remain unresolved;
- bounded reconnect-fenced item-use eligibility projection that stays dormant for unrestricted content and advances directly from committed XP results;
- read-only analytics for observed player-time, first-session cohort retention, classified Coin supply/movement including committed PvE pocket loss with explicit coverage gaps, and Bazaar current-book/execution microstructure without duplicate market state;
- deterministic zone/backend churn coverage across lifecycle status, hard-cap, stale-heartbeat and recovery transitions without inventing an autoscaling policy;
- high-cardinality managed-entity persistence coverage proving 64 independent sources through 128 rewarded lifecycle chains under 16-worker/8-connection contention while leaving real Paper TPS/MSPT/client rendering empirical;
- adversarial JDBC transaction-failure coverage proving both full rollback after a late pre-commit SQL failure and exact idempotent recovery after post-commit acknowledgement loss across Coin and multi-authority item-delivery state, with the recovered rollback sequence also passing the aggregate persistent-integrity verifier cleanly;
- operator-triggered bounded `/integrity` verification including session/transfer identity+version checks, `PVE_DEATH_LOSS` result/wallet-version/sink-ledger reconciliation, shared commodity-delivery CLAIMED↔processed-claim/player-state-version reconciliation, shared unique-delivery CLAIMED↔processed-claim/DELIVERED-provenance/item+player-version reconciliation without pinning historical custody, Auction create/settlement operation+delivery+provenance+ledger evidence, Bank balance/tier/version/interest history and loaded-tier checks, Bazaar creation/fill/remainder/cancellation evidence, Secure Trade terminal operation/frozen-escrow/delivery/provenance/ledger reconstruction, irreversible salvage request/result + serialized-state/destruction + Coin/commodity ledger + return-delivery/source-operation evidence, clan treasury transfer-result/version/arithmetic/current-balance/clan-ledger evidence, clan storage operation/commodity-history/withdrawal-delivery/unique-provenance/asset-ledger evidence, skill progression XP/cap evidence/catalog checks, renewable resource source-head/harvest/fulfillment/managed-entity evidence with unfinished fulfillment kept recoverable, Bounty start/Coin-fee/direct+managed-progress/frozen-content-version/summon-prepare+lease/materialization/terminal evidence while crash-window managed kills remain recoverable before bridge creation, Map state-coupled open/reservation/pinned-handoff/run-lifecycle evidence with pre-handoff and pre-release recovery windows preserved, personal/commission crafting source/output/XP evidence, expansion resolution/history/feature/world-era consequence checks, Artifact create/relocate/discovery source evidence and active Attunement profile catalog identity, persistent PvE Map reward and Bounty completion/pouch evidence, plus fail-closed development-route capability isolation;
- offline coherent local backup/restore scripts with manifest/checksum/version validation, a fail-closed incomplete-restore startup fence, and a bounded proxy-first logout drain before local Paper shutdown.

The highest-value remaining recovery boundary is now **empirical coherent restore proof on the actual Windows/Docker development machine**, not more backup-script architecture. Upgrade economics/power, broader Clan-War gear representation, real-client competitive feel, first-Map acquisition, concrete skill-gated item content, and the exact ordinary-PvE death-loss curve/consequences remain intentionally behind explicit content/tuning or empirical decisions rather than being guessed into architecture.

## Rule

Architecture scaffolding may be intentionally boring. Prefer small immutable records/enums/interfaces with strict validation over speculative framework abstractions. Do not create microservices/modules solely because these systems are large on the roadmap. New content should compose proven foundations rather than invent parallel authority.
