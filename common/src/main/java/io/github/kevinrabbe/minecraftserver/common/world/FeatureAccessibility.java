package io.github.kevinrabbe.minecraftserver.common.world;

/** Persistent logical accessibility; separate from whether runtime infrastructure is currently active. */
public enum FeatureAccessibility {
    LOCKED,
    AVAILABLE
}
